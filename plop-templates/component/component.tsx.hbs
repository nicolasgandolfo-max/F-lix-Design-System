"use client";

import * as React from "react";
import { cva, type VariantProps } from "class-variance-authority";
import { cn } from "../../lib/utils";

const {{camelCase name}}Variants = cva(
  "inline-flex items-center",
  {
    variants: {
      variant: {
        default: "",
      },
    },
    defaultVariants: {
      variant: "default",
    },
  },
);

/** Props for the {{name}} component. */
interface {{name}}Props
  extends React.HTMLAttributes<HTMLDivElement>,
    VariantProps<typeof {{camelCase name}}Variants> {
  // Add component-specific props here (use JSDoc for Storybook autodocs)
}

const {{name}} = React.forwardRef<HTMLDivElement, {{name}}Props>(
  ({ variant, className, ...rest }, ref) => (
    <div
      ref={ref}
      data-slot="{{kebabCase name}}"
      className={cn({{camelCase name}}Variants({ variant }), className)}
      {...rest}
    />
  ),
);
{{name}}.displayName = "{{name}}";

export { {{name}}, {{camelCase name}}Variants };
export type { {{name}}Props };
